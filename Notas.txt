-------------------------------------------------------
PASOS PARA INSTALAR Y CREAR ENTORNO VITUALES PARA PYTHON

https://kivy.org/doc/stable/installation/installation-linux.html?highlight=pip
$ sudo apt-get install -y \
    python \
    python3-pip \
    build-essential \
    git \
    python3-pip \
    python-dev \
    python3-dev \
    libsdl2-dev \
    libsdl2-image-dev \
    libsdl2-mixer-dev \
    libsdl2-ttf-dev \
    libportmidi-dev \
    libswscale-dev \
    libavformat-dev \
    libavcodec-dev \
    zlib1g-dev \

$ sudo pip3 install --upgrade pip setuptools
$ sudo pip install virtualenvwrapper
  nvim .bashrc
  #Virtualenvwrapper settings:
    export WORKON_HOME=$HOME/.virtualenvs
    export VIRTUALENVWRAPPER_PYTHON=/usr/bin/python3
    source /usr/local/bin/virtualenvwrapper.sh


01- Crear un entorno Virtual
    $ mkvirtualenv -p  /usr/bin/python3.* EV01

02- Listar todos los entornos Virtuales 
    $ lsvirtualenv 

03- Activar Entorno Virtual
    $ workon k36

04- Desactivar Entorno Virtual
    $ deactive

05- Eliminar Entorno virtual
    $ rmvirtualenv ENVNAME

-------------------------------------------------------
01- Arrancar el servidor
    python manage.py runserver

02- Crear App
    python manage.py startapp polls

03- Crear una cuenta SuperUsario
    manage.py createsusperuser
